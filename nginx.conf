server {
    listen 80;
    server_name Admin.deepmarks.ai;
    root /var/www/admin;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Gzip compression for better performance
    gzip on;
    gzip_types text/css application/javascript application/json;
}

